//693. Binary Number with Alternating Bits in java...

//Approach one !
/**class Solution {
    public boolean hasAlternatingBits(int n) {
        String s = Integer.toBinaryString(n);

        for(int i = 0; i<s.length()-1; i++){
            if(s.charAt(i) == s.charAt(i+1)){
                return false;
            }
        }
        return true;
    }
}**/



//Approach Two!

class Solution{
    public boolean hasAlternatingBits( int n ){
        int x =  n ^ (n >> 1);

        return (x & (x +1)) == 0;
    }
}
